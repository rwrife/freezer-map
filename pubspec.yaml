name: freezer_map
description: "Offline, local-first freezer inventory and planning app."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.13.2 <3.14.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: 6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
