# Platform boundary

Document-picker and local-notification adapters belong here behind application
or domain interfaces. Platform code must not contain inventory policy or import
presentation code. Baseline inventory operation must not require a permission.
